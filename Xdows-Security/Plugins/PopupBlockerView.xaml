<UserControl
    x:Class="Xdows_Security.Views.PopupBlockerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Xdows_Security.Views"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0">
            <CommandBar.Content>
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="弹窗拦截器"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Margin="12,16,0,0"
                               FontSize="16"/>
                    <ToggleSwitch x:Name="MasterToggle"
                                  Grid.Column="1"
                                  Margin="16,8,0,0"
                                  OffContent="已停止"
                                  OnContent="监控中"
                                  Toggled="MasterToggle_Toggled"/>
                    <AutoSuggestBox x:Name="PopupSearchBox"
                                    Grid.Column="2"
                                    PlaceholderText="搜索规则..."
                                    HorizontalAlignment="Center"
                                    MinWidth="240"
                                    Margin="16,12,0,8"
                                    TextChanged="PopupSearchBox_TextChanged"
                                    QueryIcon="Find"/>
                </Grid>
            </CommandBar.Content>
            <AppBarButton Icon="Add" Label="添加规则" Click="AddPopupRule_Click"/>
            <AppBarButton Icon="Delete" Label="删除规则" Click="DeletePopupRule_Click"/>
            <AppBarButton Icon="Edit" Label="编辑规则" Click="EditPopupRule_Click"/>
        </CommandBar>

        <Grid Grid.Row="1" Padding="16,8,16,4" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="标题" FontWeight="SemiBold" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="进程" FontWeight="SemiBold" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Grid.Column="2" Text="状态" FontWeight="SemiBold" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Grid.Column="3" Text="启用" FontWeight="SemiBold" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
        </Grid>

        <ListView x:Name="PopupRuleList" Grid.Row="2" SelectionMode="Single" DoubleTapped="EditPopupRule_Click">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PopupRule">
                    <Grid Padding="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{x:Bind Title}" FontSize="14" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{x:Bind ProcessName}" FontSize="14" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{x:Bind Status}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <ToggleSwitch Grid.Column="3" IsOn="{x:Bind IsEnabled, Mode=TwoWay}" Toggled="PopupRuleToggle_Toggled" MinWidth="60" HorizontalAlignment="Center"/>
                        <Grid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="编辑规则" Click="EditPopupRule_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE70F;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutSeparator/>
                                <MenuFlyoutItem Text="删除规则" Click="DeletePopupRule_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE107;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
